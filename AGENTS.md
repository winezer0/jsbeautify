**解析器代码编写规范**：

从第一性原理出发，分析代码需求，并给出最佳实践解决方案

本项目当前还处于开发阶段，不需要实现数据库迁移和旧版本兼容。

## 一、文件规范

1. 行数约束：单文件≤400行，超行按职责拆分（如nodes/edges/imports分文件）；
2. 命名规则：文件小写下划线；测试文件`xxx_test.go`；函数驼峰命名
3. 导入排序：标准库 → 第三方库 → 项目内部库
4. **使用功能分包、结构化分层、函数式编程、低耦合方法，让代码保持结构优雅和可维护**

## 二、代码编写规范

1. 嵌套限制：if/for/switch嵌套不超3层，复杂逻辑拆分子函数，杜绝深层嵌套
2. 函数约束：单一职责；参数≤5个，多参数用结构体封装；禁止滥用全局变量，数据靠参数传递
3. 封装设计：用结构体聚合数据、接口定义行为，杜绝包循环依赖
4. 输出约束：print等日志输出仅用英文文本，禁止中文输出字符串
5. 常量统一：常用字符串必须预定义常量，禁止硬编码字符串字面量


## 三、注释规范

1. 所有新增函数必须添加函数注释，格式`// 函数名 功能描述`
2. 关键逻辑步骤添加精简注释，不写冗余无意义注释
3. 需修改代码时,保留代码中原有的注释信息.
3. 默认使用英语注释，避免出现乱码问题

## 四、 单元测试强制规范

1. 代码配套：每个提取器模块必须配套`_test.go`单元测试文件
2. 数据规则：测试数据全部硬编码常量，不读取外部文件；断言优先完整字符串比对，不只用数量校验
3. 覆盖率：单元测试覆盖率≥80%；覆盖正常场景、边界值、异常分支、全部公开函数
4. 用例组织：采用表驱动测试 + `t.Run()`分子测试，用例名称清晰描述测试场景

## 核心强制红线

1. 全部模块必须写单元测试，测试数据硬编码无外部文件依赖
2. 不忽略错误、导出函数必须带函数注释、输出仅英文文本
3. 实现<SPEC>，并在实现过程中持续维护一个 implementation-notes.md 文件，记录下你在规范中未明确说明而必须做出的决策、需要修改的内容、权衡取舍以及其他我需要了解的信息。
